import ExperienceList from '@/components/resume/ExperienceList'
import ResearchList from '@/components/resume/ResearchList'
import EducationList from '@/components/resume/EducationList'

export default function AboutPage() {
  return (
    <section className="space-y-10">
      <header className="space-y-3">
        <h1 className="text-3xl font-semibold">About</h1>
        <p className="max-w-2xl">
          Full stack developer focused on clean, scalable web apps. UI and backend, strong on data, real time features, and DevOps.
        </p>
      </header>

      <ExperienceList />
      <ResearchList />
      <EducationList />
    </section>
  )
}
